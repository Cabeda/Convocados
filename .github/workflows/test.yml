name: Test

on: [push]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: npm install
      run: npm install --include=dev
    - name: Run tests
      run: npm test